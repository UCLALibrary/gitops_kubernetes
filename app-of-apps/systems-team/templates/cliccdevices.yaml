---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: clicc-devices
  namespace: argocd
  labels:
    team: systems
#  finalizers:
#    - resources-finalizer.argocd.argoproj.io
  annotations: {}
spec:
  syncPolicy:
    managedNamespaceMetadata:
      labels:
        namespace_team: systems
    automated:
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
  project: default
  destination:
    name: {{ .Values.spec.destination.server }}
    namespace: clicc-devicesprod
  sources:
    - repoURL: https://chartmuseum.library.ucla.edu
      targetRevision: 1.0.0
      chart: clicc-devices
      helm:
        valueFiles:
          - $values/charts/prod-cliccdevices-values.yaml
    - repoURL: "https://github.com/UCLALibrary/clicc-devices"
      targetRevision: main
      ref: values

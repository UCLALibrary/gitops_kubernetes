# Test Validator
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: test-validator
  namespace: argocd
  labels:
    team: services
  annotations: {}
spec:
  syncPolicy:
    managedNamespaceMetadata:
      labels:
        namespace_team: services
    automated:
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
  project: default
  destination:
    name: {{ .Values.spec.destination.rke.test.server }}
    namespace: test-validator
  sources:
    - repoURL: https://chartmuseum.library.ucla.edu
      targetRevision: 1.1.1
      chart: uclalib-helm-generic
      helm:
        valueFiles:
          - $values/apps/services-team-value-files/test-validator.yaml
    - repoURL: "https://github.com/UCLALibrary/gitops_kubernetes"
      targetRevision: main
      ref: values

# Prod Validator
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prod-validator
  namespace: argocd
  labels:
    team: services
  annotations: {}
spec:
  syncPolicy:
    managedNamespaceMetadata:
      labels:
        namespace_team: services
    automated:
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
  project: default
  destination:
    name: {{ .Values.spec.destination.rke.prod.server }}
    namespace: prod-validator
  sources:
    - repoURL: https://chartmuseum.library.ucla.edu
      targetRevision: 1.1.1
      chart: uclalib-helm-generic
      helm:
        valueFiles:
          - $values/apps/services-team-value-files/prod-validator.yaml
    - repoURL: "https://github.com/UCLALibrary/gitops_kubernetes"
      targetRevision: main
      ref: values
